const assert = require('assert');

describe('FastPay CI/CD', () => {
  it('debería ser una prueba exitosa', () => {
    console.log('FastPay CI/CD test is running');
    assert.strictEqual(1 + 1, 2);
  });
});